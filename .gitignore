# Build output
bin/
target/

# v4.3 (2026-05-01): provider routing yaml synced into pkg/providerroutes/
# at build time (see Makefile sync-fingerprint). Single source of truth
# lives in aikey-cli/data/; the pkg/providerroutes/data/ copy is intentionally
# gitignored at the workspace root, not here.

# Runtime config (use aikey-proxy.yaml.example as template)
# aikey-proxy.yaml is committed intentionally as the default config template

# Environment files
.env

# Go test cache
*.test
*.out

# OS
.DS_Store
/aikey-proxy
